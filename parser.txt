construcao arvore binaria de parsing

1 - procurar por pipes;
2 - se achar um pipe, torna-lo root;
3 - montar lado esquerdo do root;
3.1 - procurar por redirect;
3.1.1 - se achar um redirect, procurar por outro na sequencia;
4 - montar a estrutura t_exec e t_redir, apontando para t_exec;
5 - voltar para o root e montar lado direito;
6 - voltar para passo 2;


1 - FUNCAO PARSE

parametros (1 token da lista);
retorna (root da arvore);

1 - criar pointer *root tipo void;
2 - root recebe a build exec;
3 - andar com o token ate o pipe ou ate acabar a lista;
4 - se for um pipe retornar o token do pipe;
	4.1 - igual o root a build_pipe(antigo root, parse(prox token depois do pipe));
5 - se nao for pipe retornar o antigo root;